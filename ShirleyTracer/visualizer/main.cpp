#include "Bitmap.h"
#include "Jittered.h"

int main() {
	//// visualize sampler.
	//int num = 1024;
	//Jittered* s = new Jittered(1024);
	//std::auto_ptr<Bitmap> p(new Bitmap(300, 300));
	//vec3 col = white;
	//vec3 back = black;
	//int ir = int(255.99*col[0]);
	//int ig = int(255.99*col[1]);
	//int ib = int(255.99*col[2]);
	//for (int j = 0; j < 300 * 300; j++) {
	//	p->SetPixel(j / 300, j % 300, (0 << 16) | (0 << 8) | 0);
	//}
	//for (int j = 0; j < num; j++) {
	//	vec2 sam = s->sample_unit_square();
	//	int x = (int)(sam[0] * 300);
	//	int y = (int)(sam[1] * 300);
	//	p->SetPixel(x, y, (ir << 16) | (ig << 8) | ib);
	//}
	//p->SaveBMP("./results/sample.bmp");
}